{
  "name": "suf-gen",
  "version": "0.1.0",
  "main": "./dist/cli.js",
  "scripts": {
    "prepack": "suf && tsc -b",
    "link:cli": "yarn &&  tsc -b && npm link",
    "start": " tsc -w",
    "build": "yarn link:cli"
  },
  "author": "Syler",
  "license": "MIT",
  "bin": {
    "suf-gen": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/TheRealSyler/suf-gen"
  },
  "devDependencies": {
    "@types/node": "^12.12.54",
    "suf-cli": "^0.1.1",
    "@types/fs-extra": "^9.0.1",
    "typescript": "^4.0.2"
  },
  "dependencies": {
    "@sorg/log": "^2.2.0",
    "fs-extra": "^9.0.1",
    "suf-node": "^1.2.1"
  }
}
